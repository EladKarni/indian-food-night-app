"use client";

export default function GuestSetHint() {
  return (
    <p
      style={{
        color: "var(--ifn-muted)",
        fontSize: 13,
        fontStyle: "italic",
        marginBottom: 16,
      }}
    >
      You&apos;re all set for IFN. Text the host if anything changes.
    </p>
  );
}
